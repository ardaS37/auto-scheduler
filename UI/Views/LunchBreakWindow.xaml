<Window x:Class="AutoScheduler.UI.Views.LunchBreakWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Öğle Arası Ayarı"
        Height="240"
        Width="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Öğle arası saatleri (HH:mm):" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Başlangıç:" Width="90" VerticalAlignment="Center"/>
            <TextBox x:Name="StartBox" Width="100" ToolTip="Saat:Dakika biçiminde girin, örn. 12:00"/>
            <TextBlock Text="Bitiş:" Width="60" Margin="16,0,0,0" VerticalAlignment="Center"/>
            <TextBox x:Name="EndBox" Width="100" ToolTip="Saat:Dakika biçiminde girin, örn. 13:00"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Foreground="{DynamicResource AppMutedForegroundBrush}" TextWrapping="Wrap">
            Bu saat aralığı, öğle arası kuralı açıkken ders yerleştirmede otomatik olarak boş bırakılır.
            Ayrıca öğretmenin “Öğleden önce/sonra” müsaitlik ayarı bu aralığa göre uygulanır.
        </TextBlock>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="İptal" Width="90" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Kaydet" Width="90" IsDefault="True" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>

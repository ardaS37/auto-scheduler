<Window x:Class="AutoScheduler.UI.Views.TeacherDetailedAvailabilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detaylı Müsaitlik Listesi"
        Height="680"
        Width="760"
        WindowStartupLocation="CenterOwner">
    <DockPanel Margin="12">
        <Border DockPanel.Dock="Top"
                Style="{StaticResource AccentCardBorderStyle}"
                CornerRadius="10"
                Padding="10"
                Margin="0,0,0,10"
                Effect="{StaticResource CardShadowEffect}">
            <StackPanel>
                <TextBlock Text="{Binding Teacher.Name}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource AppAccentStrongBrush}"/>
                <TextBlock Text="Şablondaki gün ve ders saatlerine göre öğretmenin uygun olmadığı saatleri işaretleyin."
                           Foreground="{DynamicResource AppMutedForegroundBrush}"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Kapat" Width="90" IsCancel="True"/>
        </StackPanel>

        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="AvailabilityGrid"/>
        </ScrollViewer>
    </DockPanel>
</Window>

<UserControl x:Class="AutoScheduler.UI.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             DataContext="{Binding StudentsVm}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Öğrenciler" FontSize="26" FontWeight="Black" Foreground="{DynamicResource AppAccentStrongBrush}"/>
            <TextBlock Text="Sınıf seçip öğrencilerin ad, soyad ve okul numarası bilgilerini buradan yönetin."
                       Margin="0,6,0,0"
                       Foreground="{DynamicResource AppMutedForegroundBrush}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0,18,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Sınıf / Şube" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0"/>
                <ComboBox ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" DisplayMemberPath="Name" Width="220"/>
                <TextBlock Text="{Binding StudentCount, StringFormat='{}{0} öğrenci'}" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="{DynamicResource AppMutedForegroundBrush}"/>
                <Button Content="Öğrenci Ekle" Margin="24,0,0,0" Command="{Binding AddStudentCommand}"/>
                <Button Content="Seçiliyi Sil" Margin="8,0,0,0" Command="{Binding RemoveStudentCommand}"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Students}"
                  SelectedItem="{Binding SelectedStudent}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ad" Binding="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Soyad" Binding="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Okul No" Binding="{Binding StudentNumber, UpdateSourceTrigger=PropertyChanged}" Width="170"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
